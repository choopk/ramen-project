# CLAUDE.md

@AGENTS.md

Canonical project instructions live in AGENTS.md (imported above — don't duplicate
here; it is shared with OpenCode and every other agentic CLI). Claude-Code-specific:
follow the "Living memory & agent loop" section of AGENTS.md in interactive sessions
(read `memory/MEMORY.md` + `memory/USER.md` once at session start, write back only
when finishing, then run `scripts/memory-lint.sh`). Day-to-day guide: `memory/README.md`.
