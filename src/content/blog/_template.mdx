---
# Copy this file to `src/content/blog/<slug>.mdx` and fill it in.
# The leading underscore is excluded by the content loader (src/content.config.ts), so this
# template never loads, renders, or gets schema-validated. Your copy (no underscore) does.
#
# AUDIT GATES (pnpm run audit): each published post must pass ALL of these:
#   • Body length     ≥ 1000 words of rendered prose
#   • description      110–160 characters (SEO meta length, enforced by src/lib/seo.mjs)
#   • Images           ≥ 1 image with a non-empty alt attribute
#   • Internal links   ≥ 2 links to other posts/pages on this site
#   • Tags             ≥ 1 tag; keep draft:false only when truly ready to publish
#
title: '' # Specific, concrete headline. No clickbait.
description: '' # 110–160 chars. This is the SERP snippet. Make it earn the click.
pubDate: 2025-01-15 # REQUIRED. Replace with the publish date in YYYY-MM-DD format.
# updatedDate: 2025-01-15       # Uncomment + set when you materially revise a published post.
# heroImage: '/images/your-hero.png'   # Optional OG image + counts toward the image gate (give it alt below too).
tags: [] # ≥ 1 tag, e.g. ['styles', 'tonkotsu']
draft: true # Flip to false to publish. Drafts are hidden in prod.
author: 'Ramen Guide' # Org byline. Do not use a personal name.
---

{/* Open with the answer or the thesis. No throat-clearing. Lead with the number, the finding, the take. */}

## A descriptive H2, not "Introduction"

Write the body. Aim well past 1000 words of substance. Use primary sources and link them.

{/* IMAGE GATE: at least one image with real alt text. */}
![Describe the chart/screenshot for accessibility and SEO](/images/example.png)

{/* INTERNAL-LINK GATE: link to at least two other pages on the site. */}
See our [editorial standards](/editorial-standards/) and related coverage in
[another post](/blog/some-other-post/).

## Another section

Concrete examples, dated numbers, honest caveats.

## Bottom line

End with the takeaway a reader can act on.
